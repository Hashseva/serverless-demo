name: Trigger Jenkins

on:
  push:
    branches: main

jobs:
  trigger-jenkins:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins build
        run: |
          curl -X POST http://ip10-0-52-5-d34gvgvtq0k1c7corn4g-8080.direct.lab-boris.fr/job/serverless-demo/build --user "a:a" 
